// Placeholder test — will be replaced as systems are implemented (TASK-01-*)
#include <catch2/catch_test_macros.hpp>

TEST_CASE("placeholder — infrastructure sanity", "[sanity]") {
    REQUIRE(1 + 1 == 2);
}
